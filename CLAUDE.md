# Project Context

This is the CPM Training Clone — a vocational training tool for capital
project cost controls. The full project spec is in `SPEC.md` — read it
before doing anything substantive in this codebase.

Reference materials: a real archive export from the commercial tool we're
mirroring lives in `reference/UAT_20230427.zip`. See SPEC.md §3 for IP
boundaries (important) and §8 for the archive format.

When making decisions about architecture, schema, or features, refer back
to SPEC.md. If a new decision is made that isn't reflected there, ask the
user whether to add it to the Decisions Log (§10).