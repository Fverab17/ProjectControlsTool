# Copy this file to .env and fill in values for local overrides.
# Docker Compose already sets these via the environment: block,
# so .env is only needed if you run backend/frontend outside Docker.

DATABASE_URL=postgresql+asyncpg://cpm:cpm_dev@localhost:5432/cpm_training
DATABASE_URL_SYNC=postgresql://cpm:cpm_dev@localhost:5432/cpm_training
SECRET_KEY=change-me-in-production
VITE_API_BASE_URL=http://localhost:8000
